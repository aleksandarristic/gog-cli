# TODO

Planned features and improvements, roughly in priority order.

---

- **S3 storage backend** — support uploading backups directly to an S3 bucket
  as an alternative to a local directory destination.

- **aria2c config** — support configuring aria2c download parameters (e.g.
  connections, split, timeout) via the app config file, instead of hardcoding
  defaults when invoking aria2c.
